'use strict';

const supportedVideoExtensions = ['mp4', 'mov', 'm4v'];
const defaultInputDevice = {id: 'SYSTEM_DEFAULT', name: 'System Default'};

module.exports = {
  supportedVideoExtensions,
  defaultInputDevice
};
